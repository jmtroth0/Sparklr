<h1><%= album.id ? "Edit Album" : "Create Album" %></h1>

<p class='errors'></p>

<form>
  <label for="title">Title</label>
  <input
    id="title"
    type="text"
    name="title"
    value="<%= album.escape("title") %>">
  <label for="description">Description</label>
  <textarea
    id="description"
    type="textarea"
    name="description"><%= album.escape("description")%></textarea>
  <input
    class="submit-form"
    type="submit"
    value="Create">
</form>
